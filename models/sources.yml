version: 2

sources: 
  - name: dbt_demo_first
    database: dbt_demo
    schema: dbt_schema
    tables: 
      - name: dbt_first
        freshness:
          warn_after:
            count: 1
            period: hour
          error_after:
            count: 3
            period: hour
        loaded_at_field: BLOCK_TIMESTAMP
